# The fallback. Any biome no preset names -- a datapack's own, or one added after this build -- gets this.
# The format is documented in docs/MIRRORS.md.
Name=overworld
Base=GREEN
Layer=LIGHT_BLUE HALF_HORIZONTAL
Layer=GREEN TRIANGLES_BOTTOM
Layer=GREEN BORDER
