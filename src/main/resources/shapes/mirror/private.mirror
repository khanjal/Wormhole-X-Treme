# A bar painted across a banner. Carries no permission -- it says something, it does not enforce it.
# The format is documented in docs/MIRRORS.md.
Name=private
Base=GRAY
Layer=RED STRIPE_DOWNRIGHT
Layer=GRAY BORDER
