# A colour and a border and no charge, for when the sampled look is wrong and the build would rather the banner said nothing.
# The format is documented in docs/MIRRORS.md.
Name=plain
Base=WHITE
Layer=LIGHT_GRAY BORDER
